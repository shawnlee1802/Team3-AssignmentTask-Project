{
  "name": "assignment-tracker",
  "version": "1.0.0",
  "description": "Assignment tracker built with Node.js, Express, EJS, and MySQL.",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon app.js",
    "test": "node --check app.js && node --check db.js && node --check calendar.js && node tests/priority.test.js && node --test tests/auth.test.js"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "dotenv": "^17.4.2",
    "ejs": "^3.1.10",
    "express": "^4.19.2",
    "express-session": "^1.18.0",
    "mysql2": "^3.11.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4",
    "proxyquire": "^2.1.3",
    "supertest": "^7.2.2"
  },
  "engines": {
    "node": ">=18"
  }
}
