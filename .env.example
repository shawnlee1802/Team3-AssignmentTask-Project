PORT=3000
SESSION_SECRET=replace-with-your-own-secret

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=replace-with-your-own-mysql-password
DB_NAME=assignment_tracker
DB_CREATE_DATABASE=true

# Used by Docker Compose for its MySQL container.
MYSQL_ROOT_PASSWORD=replace-with-a-docker-root-password
MYSQL_APP_PASSWORD=replace-with-a-docker-app-password
